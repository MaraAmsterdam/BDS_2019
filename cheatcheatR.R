#  arrange() function argument .by_group = TRUE or FALSE

# removing stop words in an anti_join operation, performing sentiment analysis is an inner join operation

# select columns with select() select rows with filter()

# Steps to get tidy text format
# 1. convert to tibble
# 2. tokenize using unnest_tokens(word, text)
# 3. remove stop_words
# 4. count(words, sort = TRUE)

# sentiment analysis 

# inner_join to join on corresponding words

# Regression cheats

# caret::nearZeroVar(syldf)
# caret::finfCorrelation()
